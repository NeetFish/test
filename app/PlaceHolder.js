
'use strict';

var React = require('react');
var ReactDOM = require('react-dom');

var PlaceHolder = React.createClass({
  render() {
    return (
      <h1>Hi I am the Index!</h1>
    );
  }
});

module.exports = PlaceHolder;
